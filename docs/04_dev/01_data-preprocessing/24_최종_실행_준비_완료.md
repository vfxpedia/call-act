# 최종 실행 준비 완료

**작성일**: 2026-01-12  
**작성자**: CALL:ACT Team  
**버전**: v1.0

---

## ✅ 완료된 작업

### 1. 네이밍 수정
- ✅ `tedicard` → `teddycard` 폴더명 변경
- ✅ 모든 스크립트 경로 업데이트

### 2. 설정 파일 생성
- ✅ `config.py` 생성: 경로 및 설정 중앙 관리
- ✅ `.env`는 API 키만 관리
- ✅ 임베딩/LLM 상수는 `config.py`에서 관리

### 3. 스크립트 수정
- ✅ `11_structured_for_rag.py`: config.py 사용
- ✅ `10_generate_keywords_dict_from_hana.py`: config.py 사용
- ✅ `10_chunk_and_embed_failed_documents.py`: config.py 사용

### 4. 문서 작성
- ✅ `22_첫_실행_단계_가이드.md`: 첫 실행 위치 및 명령어
- ✅ `23_설정_파일_및_경로_관리_가이드.md`: 설정 파일 사용법

---

## 🚀 첫 실행 단계

### 실행 위치

```bash
cd C:\Users\AI-WS01\projects\call-act\data-preprocessing_dev\preprocessing\teddycard
```

### 실행 명령어

```bash
python 10_generate_keywords_dict_from_hana.py
```

### 예상 소요 시간

- 하나카드 데이터 수집: **1-2분**
- LLM 보완 (선택적): **30-60분**

---

## 📋 전체 실행 순서

### Step 1: 키워드 사전 업데이트 ✅ (첫 단계)

```powershell
cd C:\Users\AI-WS01\projects\call-act\data-preprocessing_dev\preprocessing\teddycard
python 10_generate_keywords_dict_from_hana.py
Copy-Item keywords_dict_updated.json keywords_dict.json -Force
```

### Step 2: 임베딩 에러 처리

```bash
python 10_chunk_and_embed_failed_documents.py
```

### Step 3: RAG 구조화 데이터 생성

```bash
python 11_structured_for_rag.py
```

---

## ⚙️ 설정 확인

### 1. .env 파일 (프로젝트 루트)

```env
OPENAI_API_KEY=sk-...
```

### 2. config.py (자동 설정)

- 경로: `data-preprocessing_dev/preprocessing/teddycard/config.py`
- `ENV_TYPE = 'dev'` (기본값)
- 모든 설정이 자동으로 적용됨

---

## 📝 참고 문서

- `22_첫_실행_단계_가이드.md`: 첫 실행 위치 및 명령어
- `23_설정_파일_및_경로_관리_가이드.md`: 설정 파일 사용법
- `18_키워드_임베딩_RAG_개선_실행_가이드.md`: 전체 실행 가이드

---

## ✅ 실행 준비 완료

모든 준비가 완료되었습니다. 첫 단계부터 실행하시면 됩니다!
