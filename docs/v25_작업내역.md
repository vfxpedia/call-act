# v25 작업내역

## 주요 변경사항

### 1. 다이렉트콜 WebSocket/RAG 연동
- 실전 통화(ws/call)에서 STT → RAG 파이프라인 구축
- RAGConfig: `top_k=6`, `llm_card_top_n=4`

### 2. 교육 TTS
- 교육 시뮬레이션에서 TTS 음성 출력 지원

### 3. Kanban RAG 카드
- RAG 검색 결과를 칸반보드에 카드 형태로 표시

---

## 검증 및 보완 사항 (2026-02-07)

### 확인된 항목
- audio_url: WAV 형식으로 저장 (확장자는 .mp3)
- simulation_initialized 메시지: simulation_session_id + customer_name 포함

### 수정된 항목
- edu_websocket.py RAGConfig: top_k=4 → top_k=6, llm_card_top_n=4 추가
  - call_websocket.py와 동일한 설정으로 통일

### 설정 비교 (수정 후)

| 항목 | ws/edu (교육) | ws/call (실전) |
|------|--------------|----------------|
| `top_k` | 6 | 6 |
| `llm_card_top_n` | 4 | 4 |
| `normalize_keywords` | True | True |
